/**
 *   Post Excertpt :: Wrapper
 */

// dependecies
import styled from 'styled-components';

const Wrapper = styled.article`
  border: 1px solid #ccd7dd;
  border-radius: 3px;
  padding: 1em;
`;

export default Wrapper;
