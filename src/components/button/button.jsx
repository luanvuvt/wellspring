/**
 *   Button Component
 * =============================================================================
 */

const Button = (props) => (
  <a className="btn" href="#" >
    {props.children}
  </a>
)

export default Button;
